server:
  port: 8081

spring:
  datasource:
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/xdb?useSSL=false
    driver-class-name: com.mysql.jdbc.Driver


logging:
  level:
    com.lantu: debug